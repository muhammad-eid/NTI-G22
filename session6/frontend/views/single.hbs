{{>header}}
{{>navbar}}
<section class="container">
    {{#if user}}
<div class="alert alert-success">
        <li>{{user.id}}</li>
        <li>{{user.name}}</li>
        <li>{{user.age}}</li>
        <li>{{user.email}}</li>
    </div>
    {{else}}
    <div class="alert alert-danger">
        No User found
    </div>
    
    {{/if}}
    
</section>
{{>footer}}